
initialState=[
    coords= {},
    shouldTrack=false,
    locations=[]
]

export const locationReducer = (state=initialState, action) => {
    switch(action.type){
        default:
            return state
    }
}

